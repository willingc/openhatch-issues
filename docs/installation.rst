============
Installation
============

At the command line::

    $ easy_install openhatch-issues

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv openhatch-issues
    $ pip install openhatch-issues